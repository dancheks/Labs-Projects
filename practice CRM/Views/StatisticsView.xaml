<UserControl x:Class="SqlliteViewer.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Статистика перемещений" Grid.ColumnSpan="2"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding SummaryText}" Grid.Row="1" Grid.ColumnSpan="2" Background="{DynamicResource MahApps.Brushes.Accent4}"
                   Margin="0,0,0,10" TextWrapping="Wrap"/>

            <GroupBox Header="Статусы перемещений" Grid.Row="2" Grid.Column="0" Margin="5">
            <lvc:CartesianChart Series="{Binding StatusSeries}" LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Статус" Labels="{Binding StatusLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Количество"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </GroupBox>

        <GroupBox Header="Топ товаров" Grid.Row="2" Grid.Column="1" Margin="5">
            <lvc:CartesianChart Series="{Binding TopProductsSeries}" LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Товар" Labels="{Binding TopProductsLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Количество"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </GroupBox>

        <GroupBox Header="Продажи по месяцам" Grid.Row="3" Grid.Column="0" Margin="5">
            <lvc:CartesianChart Series="{Binding MonthlySalesSeries}" LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Месяц" Labels="{Binding MonthlySalesLabels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Паллеты"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </GroupBox>

        <GroupBox Header="Активность получателей" Grid.Row="3" Grid.Column="1" Margin="5">
            <lvc:PieChart Series="{Binding RecipientActivitySeries}" LegendLocation="Right"
                          InnerRadius="100" Hoverable="True" DataTooltip="{x:Null}">
                <lvc:PieChart.AxisX>
                    <lvc:Axis Title="Получатель" Labels="{Binding RecipientActivityLabels}"/>
                </lvc:PieChart.AxisX>
            </lvc:PieChart>
        </GroupBox>

        <Button Content="Экспорт в Excel" Grid.Row="4" Grid.ColumnSpan="2"
                Width="160" HorizontalAlignment="Right" Margin="0,10,0,0"
                Click="ExportToExcel_Click"/>
    </Grid>
</UserControl>